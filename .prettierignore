node_modules
dist
build
out
coverage
.next

# Vercel's local build output. Generated bundles, not source.
.vercel
package-lock.json

# Regenerated by Next on every dev/build run — formatting it just makes the
# gate go red the next time Next rewrites it.
next-env.d.ts
